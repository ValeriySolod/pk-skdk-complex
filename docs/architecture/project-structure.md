# Project Structure

## Root

`frontend` — React application.  
`backend` — FastAPI application.  
`docs` — project documentation.  
`docker-compose.yml` — local infrastructure.

## Frontend

`src/app` — app-level setup and module registry.  
`src/pages` — route-level pages.  
`src/modules` — business modules.  
`src/shared/ui` — UI Kit.  
`src/shared/api` — shared API clients.  
`src/shared/types` — shared TypeScript types.  
`src/shared/config.ts` — frontend configuration.

## Backend

To be expanded when backend architecture is stabilized.