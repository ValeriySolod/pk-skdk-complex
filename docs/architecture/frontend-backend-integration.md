# Frontend–backend integration architecture (DEV-005 through DEV-015)

## Scope and evidence

This document records repository evidence as of DEV-005; it does not declare the business UI integrated. The audit covered `frontend/src`, frontend configuration and scripts, `backend/app/main.py`, module registration, authentication dependencies, backend contract tests, and the FastAPI OpenAPI schema generated by the application.

## Historical audited state at DEV-005

The following bullets preserve the DEV-005 baseline before the DEV-006 and DEV-007 vertical slices described below. They are historical evidence, not a statement of the current integration state.

- Authentication in `frontend/src/api/auth.ts` is the only end-to-end frontend workflow whose paths and schemas align with the backend (`POST /api/v1/auth/login`, form data, and authenticated `GET /api/v1/auth/me`). `frontend/src/main.tsx` restores the token, resolves the user, redirects anonymous users, and applies client-side role visibility. Backend authorization remains authoritative.
- `frontend/src/modules/organizations/api.ts` and `frontend/src/modules/registries/api/registries.ts` can switch to in-memory demo arrays through `VITE_DEMO_MODE`; their screens are not unconditionally integrated.
- `RegistriesPage.tsx` is hardcoded display data. Incoming registry uses the conditional API. Several other pages use local state/placeholders.
- `ShipmentsPage.tsx`, `ScannerPage.tsx`, and `SearchPage.tsx` use the Axios instance formerly defined in `frontend/src/shared/api/client.ts`. That boundary used another token key (`access_token`) and assumed the configured URL already included `/api/v1`; these calls therefore did not share the active auth session. Their paths and frontend shapes are not proven against the generated contract.
- Before DEV-005, `frontend/src/api/client.ts`, `frontend/src/api/auth.ts`, and `frontend/src/shared/api/client.ts` duplicated URL/request logic. Defaults disagreed (`localhost` versus `127.0.0.1`, API root versus server root). The ignored local `frontend/.env` is environment-specific state and is not a deployment contract; `.env.example` is the documented template.
- The package had no test or lint script. The production build (`tsc -b && vite build`) was the only configured type check.

## Contract gaps

The backend contract uses the `/api/v1` root, OAuth2 password form login, bearer JWTs, and `{detail: ...}` errors (including FastAPI's 422 issue array). Protected module routes depend on `get_current_user`; some add role dependencies. Pagination and filtering are endpoint-specific rather than one universal envelope. Frontend domain types and demo records frequently use simplified fields and must be mapped per endpoint. Client-side role checks are navigation behavior, not security. Demo credentials and the demo token do not authenticate with FastAPI, and demo mode must never be represented as integration.

`backend/app/schemas/auth.py` and the generated `app__schemas__auth__UserRead` OpenAPI component define `id`, `username`, `full_name`, `role`, nullable `department`, and `is_active`. FastAPI response serialization includes `department` as a field whose value may be `null`; the frontend `User` contract therefore requires `department: string | null` and `is_active: boolean`. The demo user follows the same shape and uses `department: null`.

## Canonical boundaries

`frontend/src/api/client.ts` is the canonical request and configuration boundary. `VITE_API_URL` is required and must be an absolute HTTP(S) URL whose normalized path is exactly `/api/v1` (for example `https://host/api/v1`). An optional supplied trailing slash is removed. Credentials, query strings, fragments, other protocols, and other paths are rejected. Missing or malformed configuration throws `ApiConfigurationError` before any network call; it is not converted to `ApiError(kind: "network")`, and there is no machine-specific fallback.

The client attaches the `pk_skdk_token` bearer token unless a caller explicitly supplies an unauthenticated token provider (login), requests JSON, parses successful JSON, returns `undefined` for 204, and throws `ApiError` for request failures. HTTP errors retain safe backend `detail`, status, and kind. Network errors are normalized without propagating low-level text. Callers own user-facing Ukrainian messages and state handling; they must not log tokens or request authorization headers. Backend 401 clears the session at the authentication orchestration boundary; 403 is an authorization failure, not a logout.

`frontend/src/pages/LoginPage/LoginPage.tsx` is the only caller of the concrete `login()` function. Its caller-level mapping presents only `ApiError(kind: "http", status: 401)` as `Невірний логін або пароль`. Network, configuration, 403, 422, other HTTP, and unknown failures remain distinct from invalid credentials and receive separate Ukrainian messages. The canonical client retains structured errors and does not contain UI-message policy.

Client tests use the Node built-in test runner and Node's TypeScript type-stripping support, avoiding another frontend testing dependency. `frontend/package.json` requires Node `>=22.6.0`, the first Node 22 release with this capability, and CI selects Node 22. The test script retains the explicit `--experimental-strip-types` flag for compatibility across supported Node 22 minors.

The older Axios boundary remains temporarily for three legacy module pages and is deprecated by this decision; it must not be used by new code. Migrating those workflows requires endpoint-specific contract work and is outside DEV-005 through DEV-009.

## First vertical slice: authenticated current-user status (DEV-006)

DEV-006 connects and explicitly presents the authenticated-session status in the application shell using `GET /api/v1/auth/me` through the canonical client. In non-demo operation it uses the real login token and maps exactly to `UserRead` (`id`, `username`, `full_name`, `role`, nullable `department`, `is_active`). Successful JSON is validated at runtime before it becomes authenticated UI state. Demo mode remains an intentional mock path and is not evidence of backend integration.

The root authentication orchestrator exposes loading and authenticated states, passes the validated user to the shell, and redirects anonymous or expired sessions to login. Only an HTTP 401 clears the stored token. HTTP 403 preserves the token and renders an authorization-specific status instead of weakening backend-authoritative access or logging the user out. Malformed success responses, network failures, configuration failures, server errors, and unexpected failures also preserve the token and render distinct retry/logout status UI. There is no meaningful empty collection state: a successful response is one user, while a missing or expired session is 401.

The shell presents the backend-provided display name, username, role, and optional department. Session requests use a shared generation guard for both UI updates and token-clearing side effects: only an active `401` may clear the session, and login retries or logout invalidate older requests. Focused tests cover the `UserRead` validator, state/error mapping, rendering text model, generation-safe session clearing, and login/application destinations. A backend contract test fixes both the complete `/auth/me` response shape and its unauthenticated 401 boundary.

## Organization units read-only slice (DEV-007)

DEV-007 changes the registered `/organizations` module page from its prior demo-oriented organization CRUD presentation to a read-only organization-unit list. With demo mode disabled, `frontend/src/modules/organizations/organizationUnitsApi.ts` requests `GET /organization-structure/units` through the canonical client, producing the backend URL `GET /api/v1/organization-structure/units` and using the active bearer token. The successful payload must be an array whose every object has exactly the `OrganizationUnitRead` fields and types: integer `id`, string `name`, nullable string `code`, nullable integer `parent_id`, and boolean `is_active`. Missing, mistyped, or additional fields are rejected before UI state is updated.

Demo mode remains an explicitly separate mock loader and never calls the real endpoint. Its records use the same frontend contract only so the read-only page can render consistently; demo execution is not backend-integration evidence. The previous organizations demo CRUD API remains legacy code but is no longer called by the registered page. DEV-007 does not add or connect create, update, delete, positions, or employee-assignment behavior.

The page presents deterministic loading, populated success, and empty success states. Errors retain distinct orchestration outcomes for expired-session 401, forbidden 403, malformed success responses, network failure, invalid API configuration, server errors, and unexpected HTTP/runtime failures. An active 401 clears `pk_skdk_token` and redirects to `/login`, following the existing session-expiration boundary; a superseded request cannot clear a newer session. A 403 and every non-401 failure preserve the token. Error states render a retry action, and generation checks prevent superseded or unmounted requests from replacing current UI state.

Focused Node tests cover exact runtime validation, the canonical relative endpoint, demo-path isolation, success/empty states, active and stale 401 side effects, token-preserving 403, and all remaining error classes. Backend contract tests fix the exact serialized list shape and unauthenticated 401 response without changing backend business behavior.

## Positions read-only slice (DEV-008)

DEV-008 preserves the DEV-007 organization-unit behavior on the registered `/organizations` page and adds a second read-only section. With demo mode disabled, `frontend/src/modules/organizations/positionsApi.ts` requests `GET /organization-structure/positions` through the canonical authenticated client, producing `GET /api/v1/organization-structure/positions`. A successful payload must be an array whose every object has exactly the `PositionRead` fields and types: integer `id`, string `title`, nullable string `code`, integer `organization_unit_id`, and boolean `is_active`. Missing, mistyped, or additional fields are rejected before UI state is updated.

Position demo records use an explicitly separate mock loader and do not invoke the canonical request path. The page independently presents positions loading, populated, empty, and retryable error states without coupling them to the organization-unit result. Its error policy distinguishes active and stale 401, 403, malformed success responses, network failure, invalid API configuration, server errors, and unexpected HTTP/runtime failures. Only an active positions request receiving HTTP 401 clears `pk_skdk_token` and redirects to `/login`; stale 401 responses and every other failure preserve the token. Generation invalidation prevents superseded or unmounted positions requests from changing state, clearing the current session, or navigating.

Focused Node tests cover the exact `PositionRead` validator, canonical relative endpoint, isolated demo dispatcher, populated and empty results, retry transitions, all error mappings, active/stale 401 side effects, and stale/unmounted suppression. Backend contract tests fix the exact serialized positions list shape and unauthenticated 401 response without changing backend business behavior. DEV-008 adds no write operation, employee-assignment integration, page redesign, dependency, or backend behavior change.

## Increment boundaries, risks, and next acceptance criteria

DEV-009 preserves the DEV-007 and DEV-008 behavior on the registered `/organizations` page and adds a third independent read-only section. With demo mode disabled, `frontend/src/modules/organizations/employeeAssignmentsApi.ts` requests `GET /organization-structure/assignments` through the canonical authenticated client, producing `GET /api/v1/organization-structure/assignments`. A successful payload must be an array whose every object has exactly the `EmployeeAssignmentRead` fields and types: integer `id`, integer `user_id`, integer `position_id`, nullable ISO `YYYY-MM-DD` strings `start_date` and `end_date`, and boolean `is_active`. Missing, mistyped, invalid-date, or additional fields are rejected before UI state is updated.

Employee-assignment demo records use an explicitly separate mock loader and do not invoke the canonical request path. The page independently presents loading, populated, empty, and retryable error states without coupling them to organization-unit or position results. Its error policy distinguishes active and stale 401, 403, malformed success responses, network failure, invalid API configuration, server errors, and unexpected HTTP/runtime failures. Only an active employee-assignment request receiving HTTP 401 clears `pk_skdk_token` and redirects to `/login`; stale 401 responses and every other failure preserve the token. Generation invalidation prevents superseded or unmounted requests from changing state, clearing the current session, or navigating.

Focused Node tests cover the exact `EmployeeAssignmentRead` validator, canonical relative endpoint, isolated demo dispatcher, populated and empty results, retry transitions, all error mappings, active/stale 401 side effects, and stale/unmounted suppression. Existing organization-unit and position tests remain in the complete frontend suite as regression coverage. Backend contract tests fix the exact serialized assignments list shape and unauthenticated 401 response without changing backend business behavior.

DEV-009 is intentionally limited to the organization-unit, position, and employee-assignment read paths and their deterministic UI states. Out of scope remain writes for those resources, UI redesign, API generation, global state libraries, backend behavior changes, auth bypasses, and normalization of unrelated frontend models.

## User Management read-only slice (DEV-011)

DEV-011 connects the registered, `SYSTEM_ADMIN`-visible `/admin` page to `GET /user-management/users` through the canonical authenticated client, producing `GET /api/v1/user-management/users`. Backend authentication and authorization remain authoritative; the frontend role gate is navigation behavior only. A successful payload must be an array whose objects contain exactly the `UserRead` fields and types: integer `id`, string `username`, nullable string `email`, and boolean `is_active`. Missing, mistyped, or additional fields are rejected before UI state is updated.

Demo users are supplied only by an explicitly separate mock loader and never invoke the real request path. The page presents loading, populated, empty, and retryable error states. Active 401 responses clear `pk_skdk_token` and navigate to `/login`; stale or unmounted 401 responses cannot clear the session or navigate. A 403 and malformed-response, network, configuration, server, unexpected, stale, and unmounted outcomes preserve the token. Generation invalidation prevents superseded or unmounted requests from changing current state.

Focused frontend coverage fixes exact runtime validation, canonical endpoint selection, demo isolation, state/error mapping, retry transitions, and request-generation safeguards. Backend coverage fixes the exact serialized users-list response and unauthenticated 401 boundary without changing backend behavior. DEV-011 adds no writes, page redesign, dependency, legacy-client migration, or authorization change.

## User Management profiles read-only slice (DEV-012)

DEV-012 preserves the complete DEV-011 users slice and adds a second independent section to the registered `/admin` page. With demo mode disabled, it requests `GET /user-management/profiles` through the canonical authenticated client, producing `GET /api/v1/user-management/profiles`. Successful data must be an array whose objects contain exactly the `UserManagementProfileRead` fields: integer `id` and `user_id`; nullable strings `display_name`, `personnel_number`, `job_title`, `phone_number`, and `notes`; boolean `is_active`; and valid ISO datetime strings `created_at` and `updated_at`. Missing, mistyped, additional, or invalid datetime fields are rejected before UI state is updated.

Profile demo records are supplied only through a separate mock loader and never call the backend. Profiles independently present loading, populated, empty, and retryable error states without coupling their result to the users section. Active 401 responses clear `pk_skdk_token` and navigate to `/login`; stale or unmounted 401 responses cannot clear the session or navigate. A 403 and malformed-response, network, configuration, server, unexpected, stale, and unmounted outcomes preserve the token. Generation invalidation prevents superseded or unmounted profile requests from changing current state.

Focused frontend coverage fixes the exact runtime contract, canonical endpoint, demo isolation, error mapping, retry transitions, and request-generation safeguards. Backend coverage fixes the exact serialized profiles-list contract, nullable values, datetime serialization, and unauthenticated 401 boundary without changing backend business behavior or authorization. DEV-012 adds no writes, redesign, dependency, unrelated client migration, or backend behavior change.

## User Management role assignments read-only slice (DEV-013)

DEV-013 preserves the independent DEV-011 users and DEV-012 profiles sections and adds a third independent section to the registered `/admin` page. With demo mode disabled, it requests `GET /user-management/role-assignments` through the canonical authenticated client, producing `GET /api/v1/user-management/role-assignments`. Successful data must be an array whose objects contain exactly the `UserManagementRoleAssignmentRead` fields: integer `id` and `user_id`; string `role_code`; nullable string `scope_type`; nullable integers `scope_id` and `assigned_by_user_id`; boolean `is_active`; valid ISO datetime string `assigned_at`; and nullable valid ISO datetime string `revoked_at`. Missing, mistyped, additional, or invalid datetime fields are rejected before UI state is updated.

Role-assignment demo records are supplied only through a separate mock loader and never call the backend. Role assignments independently present loading, populated, empty, and retryable error states without coupling their result to users or profiles. Only an active role-assignment request receiving HTTP 401 clears `pk_skdk_token` and navigates to `/login`; stale or unmounted 401 responses cannot clear the session or navigate. A 403 and malformed-response, network, configuration, server, unexpected, stale, and unmounted outcomes preserve the token. Generation invalidation prevents superseded or unmounted role-assignment requests from changing current state or causing side effects.

Focused frontend coverage fixes the exact runtime contract, canonical endpoint, demo isolation, error mapping, retry transitions, active and stale 401 handling, token preservation, and stale/unmounted suppression. Backend coverage fixes the exact serialized role-assignment list contract, nullable values, datetime serialization, and unauthenticated 401 boundary without changing backend business behavior or authorization. DEV-013 adds no writes, redesign, dependency, unrelated client migration, authorization change, or backend behavior change.

## User Management audit events read-only slice (DEV-014)

DEV-014 preserves the independent DEV-011 users, DEV-012 profiles, and DEV-013 role-assignments sections and adds a fourth independent section to the registered `/admin` page. With demo mode disabled, it requests `GET /user-management/audit-events` through the canonical authenticated client, producing `GET /api/v1/user-management/audit-events`. Successful data must be an array whose objects contain exactly the `UserManagementAuditEventRead` fields: integer `id`; nullable integers `actor_user_id` and `target_user_id`; string `event_type`; nullable string `summary`; nullable plain-object `details`; and valid ISO datetime string `created_at`. Missing, mistyped, additional, invalid-datetime, array, or non-plain-object details values are rejected before UI state is updated.

The audit-events section owns its loading, populated, empty, retryable error, and request generation independently from the other three sections. Demo data is selected only through the explicit mock path. Active 401 responses clear the token and navigate to `/login`; stale 401 responses have no side effect. A 403 and malformed, network, configuration, server, unexpected, stale-request, or unmounted-request outcomes preserve the token and cannot let an obsolete request replace current state or navigate.

Focused frontend coverage fixes the exact runtime contract, canonical endpoint, demo isolation, error mapping, retry transitions, active and stale 401 handling, token preservation, and stale/unmounted suppression. Backend coverage fixes the exact serialized audit-events list contract, nullable fields, nested JSON details, datetime serialization, newest-first ordering, and unauthenticated 401 boundary without changing backend business behavior or authorization. DEV-014 adds no writes, redesign, dependency, unrelated client migration, authorization change, or backend behavior change.

## Document Management documents read-only slice (DEV-015)

DEV-015 adds the registered `/documents` page and connects it to `GET /document-management/documents` through the canonical authenticated client, producing `GET /api/v1/document-management/documents`. Successful data must be an array whose objects contain exactly the `DocumentRead` fields: integer `id`; strings `title`, `document_type`, and `status`; nullable strings `document_number` and `description`; nullable integers `organization_id` and `owner_user_id`; and valid ISO datetime strings `created_at` and `updated_at`. Missing, mistyped, additional, or invalid datetime fields are rejected before UI state is updated.

Demo mode remains an explicitly separate mock loader and never calls the real endpoint. The page coordinator owns generation-safe loading, retry, expired-session navigation, and stale or unmounted result suppression. The page uses shared UI components to present loading, empty, populated table, and retryable error states.

Focused frontend coverage fixes the exact runtime contract, canonical endpoint, demo isolation, state and error mapping, active and stale 401 handling, retry transitions, page registration, and stale/unmounted suppression. Backend coverage fixes persisted-list behavior, nullable metadata, exact serialization, and the unauthenticated 401 boundary without changing backend behavior or authorization. DEV-015 adds no document writes, attachments, versions, workflow actions, dependency, architectural change, or unrelated client migration.

Operational prerequisites remain a migrated database with an active user, a valid CORS origin for the deployed frontend, secure HTTPS deployment, a supplied `VITE_API_URL`, and demo mode disabled outside intentional demos. Automated coverage does not replace a manual deployed-browser check with a real backend session.
